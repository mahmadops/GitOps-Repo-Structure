apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nginx-chart.fullname" . }}-config
  labels:
    {{- include "nginx-chart.labels" . | nindent 4 }}
data:
  APP_NAME: "Nginx Application"
  APP_VERSION: "{{ .Chart.AppVersion }}"
  ENVIRONMENT: "Development"